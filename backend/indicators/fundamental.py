from typing import Dict, Any


def compute_basic_fundamentals(raw: Dict[str, Any]) -> Dict:
    # Placeholder to map Twelve Data fundamentals response later
    return {
        "pe_ratio": None,
        "pb_ratio": None,
        "eps": None,
        "market_cap": None,
        "dividend_yield": None,
    }


